# Formal systems studio

Three workshops, one engineering:

```
stipulated V    declared G    finite θ
```

```
python -m studio            # all workshops that finish fast
python -m studio.algebra
python -m studio.logic
python -m studio.calc
```

Not terrain. A map of cuts. Reification audit: `docs/AUDIT.md`.
PL is already in `work/pl-kernel`. You do not add it from elsewhere.

Nouns here are labels. They compress a description. They do not
report an ontological container.
