# What's in this tree

You do not need to add PL separately. The operator lives here.

```
studio/           algebra + logic + calc workshops
docs/             AUDIT, ENGINEERING, ALGEBRA, GUARDRAILS, SITE
work/
  PROCESS.py      one-file kernel resume
  pl-kernel/      reading, certify, migrate, forge, prescreen
  carriers/       CL2 L3 K3 PROC JSON
  process-calc/   six-move course + labs
  formal-engineer/ census, worlds, ghosts, calc history
  migrate.py forge.py phys2d.py gamephys.py doomtic.py games/
  insights.json
```

Nouns in this tree are labels. They compress a cut. They do not
report a container in the world.
